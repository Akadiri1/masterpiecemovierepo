Vagrantfile
/www/phpmyadmin
.vagrant/
/**www/uploads/**/
.env/config.php
ngrok.exe
.htaccess
# Per-member recommendation cache (personal viewing data; rebuilt as needed)
v1/cache/recs/
# Written by the Docker build to stamp each deploy; never committed
.build-version
